#Use an external module of your intrest and print the output of a function from that module. You can use any module of your interest. You can also use the pip command to install any external module.

import pyttsx3
engine = pyttsx3.init()
engine.say("RADHE RADHE")  
engine.runAndWait()